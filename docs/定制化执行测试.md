自动化执行定制化：
    - 模块的定制化(筛选)
    - 接口的定制化(筛选)
    - 用例的定制化(筛选): 靠ddt给pytest的数据驱动的参数内容

pytest定制化执行：使用参数去执行
 1. -m  mark--打标签-----需要测试人人员打 
    多个标签  ‘-m’ ‘a or b’  ; 只是不运行a，其他都运行  '-m' ‘not a’
 2. 有些同事感觉-m 度需要自己写，是否有那种现成的用法
    - k  模糊匹配用例名  test_login.py    -k  login
 3. 是否有层级关系
    测试文件::测试类:: 测试方法